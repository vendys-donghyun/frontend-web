---
id: button
title: Button
sidebar_position: 1
---

import { Button } from '@vendys/ui';
import { LoadingDemo } from '@site/src/components/Demos';

# Button

한 화면에 fill(primary) 버튼은 1개가 원칙입니다.

## Variant

<div className="demo">
  <Button>저장</Button>
  <Button variant="weak">더 보기</Button>
  <Button variant="secondary">취소</Button>
  <Button variant="ghost">건너뛰기</Button>
  <Button variant="danger">삭제</Button>
  <Button disabled>비활성</Button>
</div>

```tsx
<Button>저장</Button>
<Button variant="weak">더 보기</Button>
<Button variant="secondary">취소</Button>
<Button variant="ghost">건너뛰기</Button>
<Button variant="danger">삭제</Button>
```

## Size

sm 32 / md 40(기본) / lg 48. 모바일 주요 동작은 lg를 씁니다.
lg는 radius도 12px로 커집니다.

<div className="demo">
  <Button size="sm">작게</Button>
  <Button>기본</Button>
  <Button size="lg">크게</Button>
</div>

## Loading

비동기 동작 중에는 스피너가 문구 위에 겹쳐 표시되고 클릭이 차단됩니다.
문구를 바꾸지 않아도 되고, 버튼 폭은 절대 변하지 않습니다.
**300ms 안에 끝나는 작업은 스피너가 아예 나타나지 않습니다** - 짧은 작업에서 깜빡임을 막기 위해서입니다 (클릭 차단은 즉시 적용).

<div className="demo">
  <LoadingDemo />
</div>

```tsx
<Button loading={saving} onClick={save}>
  저장
</Button>
```

## Props

| prop | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `variant` | `'fill' \| 'weak' \| 'secondary' \| 'ghost' \| 'danger'` | `'fill'` | 시각적 위계 |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | 높이 32 / 40 / 48 |
| `loading` | `boolean` | `false` | 스피너 + 클릭 차단, `aria-busy` 적용 |

그 외 `button` 기본 속성을 모두 받습니다. `ref` 전달을 지원합니다.

## Copy Guidelines

- 버튼 문구는 누르면 일어나는 일을 그대로 씁니다: "저장", "정산 확정", "조회".
- 결정을 요구하는 순간(삭제·확정 등)에는 "확인", "예"처럼 무슨 일이 일어나는지 알 수 없는 문구를 쓰지 않습니다.
- 단순 안내를 닫기만 하는 버튼은 "확인"을 써도 됩니다.
